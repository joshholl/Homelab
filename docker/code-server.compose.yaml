version: "3.8"

services:
  code-server:
    image: lscr.io/linuxserver/code-server:latest
    container_name: code-server
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - PASSWORD=change-me         # or use HASHED_PASSWORD
      - DEFAULT_WORKSPACE=/config/workspace
    volumes:
      - /mnt/verkamaedhr/docker/code-server/config:/config
      - /mnt/aegir:/
